# PROGRESS

이 파일은 자동 빌드 루프 간 공유 상태다. 새 이터레이션은 여기부터 읽는다.

마지막 갱신: 2026-07-28 14:35 KST

---

## 이번에 다룬 문제 (사용자 신고 3건)

| # | 신고 | 판정 | 상태 |
|---|---|---|---|
| 1 | 금일 장 시작 30분 전 텔레그램 브리핑이 안 옴 | **재현·원인 확정** — GitHub 스케줄러 지연 | 코드 수정 완료, 실전 검증 대기 |
| 2 | 웹 브리핑이 월요일에서 멈춤 | #1의 **결과** (웹 배포·캐시는 정상) | 해소 (2026-07-28 브리핑 생성·커밋) |
| 3 | 다른 뉴스 수집은 정상인지 | **정상** — 20개 중 1개 피드만 죽어 있었음 | 죽은 피드 제거 완료 |

### 1. 브리핑 미발송 — 원인

`gh run list` 로 확인한 실제 실행 기록:

```
brief-kr  23:11 UTC 예약 → 00:09:40Z 시작 (+58분) → 09:09 KST → 게이트가 차단
brief-kr  23:23 UTC 예약 → 00:14:22Z 시작 (+51분) → 09:14 KST → 게이트가 차단
```

코드 버그가 아니다. 발송 창(08:05~08:35 KST) 게이트는 **설계대로** 동작했고,
개장(09:00) 후 발송을 막았다. 문제는 **슬롯이 2개뿐이었다**는 것.
이 저장소의 스케줄러 지연 실측은 +6 ~ +202분으로 널뛴다:

| 워크플로 | 예약 | 실제 시작 | 지연 |
|---|---|---|---|
| pulse (07-26) | 08:00Z | 08:06:54Z | +6분 |
| brief-kr (07-28) | 23:23Z | 00:14:22Z | +51분 |
| brief-kr (07-28) | 23:11Z | 00:09:40Z | +58분 |
| pulse (07-27) | 00:00Z | 01:54:55Z | +114분 |
| brief-us (07-27) | 12:30Z | 15:23:02Z | +173분 |
| data-sync (07-27) | 07:30Z | 10:52:43Z | +202분 |

옛 슬롯 배치는 0~240분 지연 중 **30개 구간(12%)만** 커버했다. 나머지는 그날 미발송.

### 2. 웹 브리핑 정지 — 원인

배포·캐시 문제가 아니다. 확인 결과:

- `https://stock-pulse-kappa-eight.vercel.app/data/manifest.json` 이 레포 최신 커밋과 일치 (Vercel 정상)
- `web/src/lib/api.ts` 는 `cache: "no-cache"` 로 fetch (캐시 정상)
- 웹은 `data/brief/latest-KR.json` 을 그대로 그린다 → **#1로 파일이 안 생겨서 월요일에 멈춘 것**

#1을 고치면 함께 해결된다.

### 3. 뉴스 수집 — 확인 결과

`python -m pipeline.run doctor` 실행:

- 매크로 12/12, 가격·상장목록 정상
- RSS **19 정상 / 1 죽음** — `yna_market`(연합뉴스 market.xml)만 빈 응답
- `data/news/latest.json` 400건, 16개 매체, 최신 기사 2026-07-27T22:29Z → 정상 갱신 중
- pulse(2시간마다) / data-sync(하루 2회) 모두 커밋 기록 정상

---

## Done

- **`pipeline/gate.py` 신설** — 발송 판정(수동 강제 → 휴장일 → 중복 → 발송 창)을
  셸에서 파이썬으로 옮겨 테스트 가능하게 했다. `python -m pipeline.run gate --market KR`
  이 stdout 에 `true`/`false`, stderr 에 이유를 낸다. **표준 라이브러리만** 쓰므로
  `pip install` 전에 돌릴 수 있다.
- **발송 창을 설정으로** — `config.BRIEF_WINDOW` = 개장 75분 전 ~ 20분 전.
  KR `07:45~08:40 KST`, US `08:15~09:10 ET` (기존 30분 → 55분).
- **슬롯 재배치** — KR 2개 → **7개**(19:20~23:20 UTC, 40분 간격),
  US 4개 → **9개**(08:40~14:00 UTC, 40분 간격, EDT/EST 양쪽 커버).
  슬롯 간격(40분) < 발송 창(55분)이라 지연 0~240분 전 구간이 덮인다.
- **브리핑에서 `--rebuild` 제거** — `pulse --market X` → `brief --send` 로 바꿔
  잡이 11~19분 → 2~3분. 슬롯을 3.5배로 늘렸는데도 월 사용 분은 ~2,370 → ~2,128분으로 감소.
  (스펙 §12가 예전부터 1순위 완화책으로 지목하던 항목)
- **브리핑 concurrency 분리** — `stock-pulse-brief-KR|US`. 예전엔 `stock-pulse-data` 를
  공유해 26분짜리 data-sync 뒤에 큐잉되면 발송 창을 통째로 놓칠 수 있었다.
- **모든 스케줄 잡에 `checkout ref: main`** — 스케줄 트리거는 *예약 시점* SHA 를 준다.
  최대 3시간 묵은 트리로 중복 판정·커밋하던 문제.
- **`.github/scripts/commit-data.sh` 신설** — 네 워크플로의 커밋·푸시를 통일.
  거절 시 `git rebase -X theirs origin/main` 후 재시도(5회). 기존 `git pull --rebase`
  3회 재시도는 충돌이 나면 그대로 실패해 데이터를 날릴 수 있었다.
- **발송 실패 시 exit 1** — `_cmd_brief` 가 텔레그램 실패를 조용히 넘기지 않는다.
  커밋 스텝에 못 가므로 브리핑 파일이 안 올라가고 → 남은 슬롯이 자동 재시도한다.
- **죽은 피드 `yna_market` 제거** — 이후 doctor 기준 KR 피드 9/9 정상.
- **테스트 53개 신설** (`tests/`, pytest)
  - `test_schedule.py` — 워크플로 cron 을 직접 파싱해 **지연 0~240분 전 구간**에서
    발송 슬롯이 존재하는지 검증. 실측 지연(+6/+51/+58/+89/+114/+202)도 개별 케이스로 고정.
    미장은 EDT/EST 양 계절 각각. cron 을 잘못 건드리면 여기서 먼저 깨진다.
  - `test_gate.py` — 창 경계·휴장일·주말·중복·`--force`. 실제 사고 시각(09:09 KST,
    11:23 ET)이 차단되는지 포함.
  - `test_workflows.py` — YAML 파싱, `ref: main`, concurrency 분리, `--rebuild` 재유입 금지.
- **`.github/workflows/test.yml`** — `pipeline/**`·`tests/**`·`brief-*.yml` 변경 푸시에서만
  실행(paths 필터). data 봇 커밋으로는 안 돈다.
- **문서 갱신** — `md파일/04-알림과스케줄.md`(시간표·휴장일 판정 위치 정정),
  `md파일/10-전체기술스펙.md`(§4.1 §5.1 §9.1 §9.1.1 §12 전면 개정).
- **2026-07-28 국장 브리핑 JSON 생성·커밋** — 웹의 "월요일에서 멈춤"을 즉시 해소.
  텔레그램은 **보내지 않았다** (이미 개장 후 09:36 KST — "개장 후 발송보다 미발송이 낫다"는
  프로젝트 기존 판단을 따름).

### 개정 중 잡은 버그

커밋 스텝을 `run: bash ... "chore(brief): KR ..."` 한 줄로 줄였다가 따옴표 밖의 `: ` 로
**네 워크플로 전부 YAML 파싱 불가**가 됐다. GitHub 은 이런 파일을 조용히 무시한다.
블록 스칼라(`run: |`)로 고쳤고 `tests/test_workflows.py` 가 재발을 막는다.

---

## In progress

### CI 에서 이미 확인한 것 (2026-07-28)

| 확인 | 방법 | 결과 |
|---|---|---|
| 워크플로 YAML 이 GitHub 에 등록되는가 | `gh workflow list --all` | 5종 전부 `active` |
| `commit-data.sh` 가 실제로 커밋·푸시하는가 | `pulse.yml` 수동 dispatch | `push 성공 (시도 1/5)` |
| 게이트가 `pip install` 전에 도는가 | `test.yml` 의 `/usr/bin/python3` 스텝 | KR/US 모두 정상 판정 |
| **미장 브리핑 경로가 러너에서 도는가** | `brief-us.yml` 리허설 dispatch | **37초, 실제 브리핑 렌더 성공** |
| **국장 브리핑 경로가 러너에서 도는가** | `brief-kr.yml` 리허설 dispatch | **37초, 실제 브리핑 렌더 성공** |
| 리허설이 커밋을 건너뛰는가 | 두 런 모두 커밋 스텝 | `-` (스킵) 확인 |
| `--send` 인터폴레이션이 맞는가 | KR 런 로그 | `brief --market KR ` (`--send` 없음) 확인 |
| 테스트 62개 | `test.yml` | 통과 |

두 워크플로 파일을 따로 손으로 고쳤으므로 각각 CI에서 확인했다. KR 리허설 로그:
게이트 `수동 실행 — 시각·중복 검사 생략`, 피드 `9 live / 0 dead`(yna_market 제거 반영),
국장 브리핑 렌더 정상.

즉 **남은 미검증 구간은 `telegram.send()` HTTP 호출 하나뿐**이고, 그 코드는 이번에
손대지 않았으며 2026-07-27 15:34Z 에 마지막으로 성공했다.

### 남은 것 — 실전 발송 검증

개정된 슬롯으로 **실제 텔레그램 발송이 나간 적은 아직 없다.** 다음 두 번이 첫 검증이다:

- **미장 브리핑: 2026-07-28 08:15~09:10 ET (= 21:15~22:10 KST)** ← 가장 먼저 온다
- **국장 브리핑: 2026-07-29 07:45~08:40 KST**

---

## Next steps

1. **다음 이터레이션에서 발송 결과부터 확인할 것.**
   ```bash
   gh run list --workflow brief-us.yml --limit 10 \
     --json databaseId,status,conclusion,createdAt
   gh run view <id> --log | grep -i "gate\|발송 창\|전송"
   git log --oneline -5 -- data/brief/
   ```
   - 커밋 `chore(brief): US 2026-07-28` 이 있고 텔레그램이 왔으면 #1 해결 확정 →
     이 파일 첫 줄을 `STATUS: COMPLETE` 로 바꾼다.
   - 안 왔으면 어느 슬롯이 언제 깨어났는지 로그로 확인하고, 지연이 240분을 넘었다면
     `brief-*.yml` 앞쪽에 슬롯을 더 깔고 `tests/test_schedule.py::MAX_DELAY_MIN` 을 올린다.
2. ~~브리핑 잡 소요 시간 실측~~ → **완료: 37초**(과금 1분). 창 55분에 견줘 충분히
   빨라서 창 끝 슬롯도 개장 20분 전에는 도착한다.
3. ~~Actions 사용 분 확인~~ → **완료: 실측 ~1,770분/월** (무료 2,000분, 여유 12%).
   문서의 옛 추정 2,128~2,370분은 틀렸다 — `pulse`는 런당 2분이 아니라 1.2분,
   브리핑은 3분이 아니라 1분이다. **당장 완화 조치가 필요 없다.**
   `data-sync`가 런당 22.4분으로 전체의 56%를 쓰므로, 나중에 줄여야 한다면 거기부터다.
4. ~~문서 드리프트 정리~~ → **완료.** 스펙 §14.2 의 드리프트 18행 중 운영자가 보고
   행동하는 7행을 해소했다(피드 목록, 튜닝 상수 기본값, 워크플로 시간표, 휴장일 판정
   위치, Actions 사용 분, 99 이력). 남은 11행은 02/03 설계문서 ↔ 구현 차이라
   기계적으로 검증할 수 없어 표로만 남겼다.
   피드 목록과 상수 기본값은 이제 `tests/test_docs.py` 가 CI 에서 강제한다 —
   `yna_market` 을 지우면서 `01-데이터소스.md` 갱신을 빠뜨린 게 계기다.
5. **여유 12% 관리** — 수동 dispatch도 같은 지갑에서 나간다. 디버깅에 `data-sync`
   (런당 22분)를 쓰지 말고 리허설(`-f send=false`, 1분)을 쓸 것.
6. **2027년 휴장일** — `util/dates.py` 는 `KR/US_HOLIDAYS_2026` 만 갖고 있다.
   해가 바뀌면 2027년 목록으로 갱신해야 휴장일에 브리핑이 나가지 않는다 (스펙 §14.1).

---

## 검증 방법 메모

```bash
python -m pytest tests -q                    # 57개, 서드파티는 pytest+pyyaml 만
python -m pipeline.run doctor                # 데이터 소스 헬스체크
python -m pipeline.run gate --market KR      # 지금 발송해야 하는지 (stdout: true/false)
python -m pipeline.run brief --market KR     # 미리보기 (⚠️ 아래 주의)

# 브리핑 경로를 러너에서 확인 — 텔레그램 발송도 커밋도 안 한다 (런당 1분)
gh workflow run brief-us.yml --ref main -f send=false
```

> ⚠️ `brief` 는 `--send` 없이도 `data/brief/{세션}-{시장}.json` 을 쓴다.
> 미리보기 결과를 커밋하면 그날 브리핑이 중복 게이트에 걸려 **조용히 안 나간다.**
> 미리보기 후엔 `git checkout -- data/brief/` 로 되돌릴 것.
