# 테스트 전용. requirements.txt 와 **함께** 설치해야 한다 —
# tests/test_manifest.py 가 build.site_data 를 임포트하고 그 체인이 pandas 를 끌어온다.
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pyyaml>=6.0
